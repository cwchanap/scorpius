# HPA-480 foundation validation

Validation scope: Task 1.2/1.3 foundation and the review-fix rounds, recorded
2026-09-08 on the `hpa-480-ui-visual-parity` worktree. This note covers the
shared canvas, exact-font plumbing, marker ownership, and headless UI picking
seam. Task 3's production battlefield and mesh cutover remain out of scope.

## Headless evidence

The resize proof uses Bevy's real `UiPlugin`, `PickingPlugin`, and
`InteractionPlugin` with the minimum asset/text/input resources they require.
It spawns a real `CanvasRoot` and `Node` stage, lays the stage out through the
UI pipeline, sends a synthetic pointer move through the picking backend at
1920x1080, resizes to 1600x1000, refreshes `UiScale` before the backend, and
verifies that the same authored `GridPos(4, 7)` is recovered from the actual
`HitData.position` and computed stage size in `HoverMap` after the resized
layout settles. The fixture is
`resize_recomputes_scale_before_picking_and_preserves_stage_cell_hits` in
`tests/ui_layout.rs`; it does not start a window, renderer, or `DefaultPlugins`.

The focused camera ownership regression is
`battle_teardown_preserves_unowned_cameras` in `src/app.rs`. It proves that
battle teardown removes the marked `BattleCamera` while preserving an
unowned camera.

Recorded commands and results:

```text
rtk cargo fmt --check
PASS (exit 0)

rtk cargo test --test ui_layout
PASS — 8 passed, 0 failed

rtk cargo test --test presentation_app
PASS — 23 passed, 0 failed

rtk cargo test --all-targets
PASS — 258 passed, 0 failed (7 suites)

rtk cargo clippy --all-targets --all-features -- -D warnings
PASS — no issues found (exit 0)
```

## Native foundation proof

Observed on 2026-09-08 after the desktop was unlocked. Native GPU execution
required an escalated launch because the sandbox could not discover Metal.
The production game opened at 1280x720 content size: Title typography was
readable, Continue reached Mission 1 Story, Briefing, and Battle, and a
Vanguard cell click updated the selected-unit HUD. Continue read the existing
save; no New Game, purchase, or reward action was exercised.

At commit `9695374`, a temporary native example rendered the production font,
icon, glyph, and board-atlas helpers in a 1280x720 content window titled
`Scorpius — HPA-480 native foundation proof`. CUA visual inspection confirmed
readable bundled typography, four distinct source glyph shapes, recognizable
Move/Attack/Guard/Counter icons, diamond tiles, a complete raised blocker, and
an upright token with its separate shadow. The first proof exposed a blocker
packing overlap; the corrected proof no longer shows the cyan strip beside
the token. Pixel isolation separately verifies the shadow contains no blocker
pixels. The temporary example was removed after building; no capture framework
was added to the application or normal test graph.

This completes the early foundation rendering proof, not full visual parity.
The production battlefield still uses the legacy renderer until Task 3.
Final source comparison, motion, and complete campaign acceptance remain open.
The browser rejected the local reference HTML URL under its security policy;
an allowed source access path or supplied reference captures is still required
for the final source comparison gate.

## Review-fix round 2

The resize fixture now uses the prescribed 1008x764 authored battle-stage
geometry at `(456, 204)` and points at the center of `GridPos(4, 7)`. It reads
the real UI picking `HitData.position` for `StageProbe`, converts the normalized
node-local hit through the observed `ComputedNode.size` and current `UiScale`,
then calls the production `grid_from_stage_point`. Both the 1920x1080 and
1600x1000 passes assert `GridPos(4, 7)`, so a shifted hit that merely remains
inside the generic stage would fail.

Recorded round 2 commands and results:

```text
rtk cargo fmt --check
PASS (exit 0)

rtk cargo test --test ui_layout resize_recomputes_scale_before_picking_and_preserves_stage_cell_hits
PASS — 1 passed, 0 failed (7 filtered out)
```

## Review-fix round 3

The unit-token theme mapping now carries separate `glyph_rect` and
`footprint_rect` values. The exhaustive archetype match uses the six exact
updated-source glyph shapes/colors, records explicit same-language choices for
the five later archetypes, and keeps the shared board shadow as the footprint.
The Interceptor uses the source `#8fe0c4` player color; it does not reuse the
mint palette constant.

The UI atlas is now 512x512. White masks occupy slots 51–54 for the four unit
shapes, slots 55–61 for the exact source D.* action paths, and slot 62 for the
exact static Back path. `D.chev` remains the forward submenu trail. The c546c87
slots 0–50 were preserved byte-for-byte (51/51), all 12 new slots are
nonblank, and corrected `assets/ui/board.png` has hash
`a548ed6410a837c7662d7f6cb42f0e12b9baa32a8a668508ef5eb903f8a394b0`. The new
icons hash is
`10da4f127bcbcafc293cc1d35a6dda27edb0faede8eb487a4dcb52331615827b`.
Source paths and slot provenance are recorded in
`docs/references/hpa-480/reference-manifest.json`.

Round 3 headless verification:

```text
rtk python3 /private/tmp/hpa480_compose_atlas.py
size= (512, 512)
preserved_slots=51/51
new_slots_nonblank=12/12
slot63=unused
sha256= 10da4f127bcbcafc293cc1d35a6dda27edb0faede8eb487a4dcb52331615827b

rtk cargo test --lib presentation::theme
cargo test: 2 passed, 174 filtered out (1 suite, 0.00s)

rtk cargo test --test ui_layout
cargo test: 8 passed (1 suite, 0.05s)

rtk cargo test --test presentation_app
cargo test: 23 passed (1 suite, 0.02s)

rtk cargo fmt --check
PASS (exit 0)

rtk cargo clippy --all-targets --all-features -- -D warnings
cargo clippy: No issues found
```

The subsequent native foundation proof above verifies the new masks after
round 4 corrected the board packing.

## Task 3 native checkpoint — 2026-09-08 (incomplete)

The initial `4d9896e` smoke binary crashed during Bevy schedule initialization
with B0001. `DamageNumberQuery` and `UnitVisualQuery` both mutably accessed
`Node` without disjoint filters. Fix `ecb25aa` adds the missing exclusion;
the rebuilt app reached Title without the panic. The implementer recorded
265 passing headless tests and a clean Clippy run for that fix.

Using the production plugin with an isolated smoke save, the coordinator
observed Title → all three Mission 1 story lines → Briefing → Battle in a
1280×720 content window. The native input path handled activation through
a visible cell wing, movement, token attack targeting, Focus, Guard, Finish,
and Resolve. A blocked destination was correctly rejected. The run reached
round two with one Artillery remaining and the Turnabout bonus complete.
It has **not** reached Victory.

This observation used the build preceding `38fbcf5`. Opaque yellow/red
rectangles appeared behind transparent diamond markers. That follow-up
removes their explicit background colors and retains `ImageNode.color`;
the rebuilt appearance still needs native verification. HP fills also
extend beyond the right card edge and remain an open visual finding.

Computer-use input subsequently returned `noWindowsAvailable` while the
app still appeared in the running-app inventory and screenshot observations.
Refreshing its app handle and raising the window did not restore clicks.
The user was asked to bring the smoke window to the foreground. The
Mission 1-to-Victory gate before Task 4 remains open; this checkpoint is
neither a native gameplay pass nor final visual parity acceptance.

Follow-up `1720b87` also makes armed Move highlights use the active unit
after inspecting an enemy. Its focused regression and all 25 presentation
tests pass; formatting and diff checks pass. This change has not yet been
observed in the running native instance.

## Native access retry and remaining findings — 2026-09-08

Commit `8c40869` resolves the recorded HP-bar overflow and locked Pointer
Out cleanup. The fill is now a child of its existing 62px clipped bar;
production pointer-exit tests confirm hover and preview clear during
playback. All 36 presentation/UI tests, formatting, diff checks, and
Clippy pass. A scoped reviewer found both fixes addressed with no new
Critical/Important breakage.

The temporary native smoke example was rebuilt from `8c40869` and removed
after compilation. A fresh `Scorpius-Native-Check.app` wrapper uses a stable
copy of that executable. It reached Title and rendered the isolated save's
Continue button, but clicking Continue again returned `noWindowsAvailable`.
The earlier wrapper also returned `procNotFound` during recovery, and a
Dock recovery attempt timed out. A fresh wrapper did not resolve input.
No Victory, final board appearance, or native gate pass is claimed.

## Task 4 headless and native startup checkpoint — 2026-09-08

The user approved proceeding with Task 4 while the earlier native Victory
gate remains open. Task 4 initial implementation is `4eb47eb`; fix round 1
is `eb7014f`. The fix covers ready-token activation, source inspector/weapon/
threat components, and real Victory/Defeat result fixtures.

The covering command was `rtk cargo test --test ui_interaction --test ui_layout
--test ui_snapshots --test presentation_app --lib`: 225 passed, zero failed.
Formatting, diff checks, and Clippy with all targets/features and warnings
as errors passed. Scoped re-review confirmed all three findings addressed
with no new Critical/Important breakage.

The corrected temporary `hpa480_hud_proof` example was built from `eb7014f`
and its temporary source removed. It waits for assets, invokes production
campaign actions against an isolated smoke save, then selects Vanguard.
A direct native startup run emitted these markers without a startup panic:

```text
[HPA-480 HUD PROOF] Title -> Continue
[HPA-480 HUD PROOF] Story -> Skip (last line)
[HPA-480 HUD PROOF] Story -> Briefing (Skip transition applied)
[HPA-480 HUD PROOF] Briefing -> Battle
[HPA-480 HUD PROOF] Battle -> Vanguard selected and activated at GridPos { x: 4, y: 7 }
```

The coordinator stopped only that proof process with Ctrl-C. Computer use
reported the Mac locked on two fresh checks, so no screenshot was inspected.
This startup check does not establish native pointer behavior, visual parity,
or Mission 1 Victory. The Task 4 native interaction gate remains required
before capture automation.

## Task 4 native inspector follow-up — 2026-09-08

After the Mac was unlocked, the `eb7014f` native HUD proof rendered Battle.
Its inspector stats and state icons overflowed horizontally into the stage.
The panel used the default row direction. Commit `7cd91e6` sets that parent
to `FlexDirection::Column`; a computed-layout bounds regression and all
226 focused tests pass, as do formatting, diff checks, and Clippy. A scoped
review confirmed the fix with no new Critical/Important breakage.

A fresh `Scorpius-HUD-Fixed.app` wrapper used the rebuilt `7cd91e6` proof
binary. Native inspection at 1280×720 confirmed portrait, HP/EN, stats, and
state icons remain inside the left sidebar. This closes the observed
inspector overflow. The current board diamonds, clipped token HP bars,
and threat cards were also visible; this is not full visual acceptance.

Clicking Move still returned `noWindowsAvailable` on both the earlier and
fresh wrappers. Raising the earlier window did not restore clicks; keyboard
Move calls returned without a visible transition. A manual Move click was
requested to distinguish tool input failure from game behavior. No manual
result or native Victory has been observed. The native interaction gate
before Task 5 remains open.

## Native Mission 1 Victory — 2026-09-09

Computer-use input recovered after resetting the CUA session and selecting
`/private/tmp/Scorpius-HUD-Fixed.app`. Its executable is the stable clone of
the `7cd91e6` native HUD proof; the current branch at run start was
`afbb37a` (documentation only after that code). The proof uses the production
plugin and an isolated save. It enters Mission 1 and activates Vanguard;
all subsequent combat below was performed through native pointer/keyboard
input, not the scratch driver.

Observed at 1280×720 content size:
- Move opened targeting. Clicking occupied Striker (4,6) was rejected and
  retained targeting; pointer Cancel restored Root.
- Vanguard moved (4,7)→(3,6), used Pile Lance on Striker (12→6 HP), selected
  Guard, and Wait automatically opened Gunner's Root menu.
- Gunner moved (3,8)→(4,7), used Focus and Rail Rifle on the right Rifleman
  (9→3 HP), then Guard/Wait handed off to Interceptor.
- Interceptor moved (5,8)→(7,7), used Pulse Carbine to defeat that Rifleman,
  then Guard/Wait. Space resolved the committed enemy phase and playback.
- In round two, clicking Vanguard's token began activation. Inspecting
  Striker left command authority with Vanguard: its Pile Lance defeated
  Striker (2 HP after resolution). Move to (1,6) was rejected; (2,7) worked.
  Guard/Wait handed off normally.
- Gunner defeated the left Rifleman (6 HP) with Rail Rifle and moved
  (4,7)→(4,5), then Guard/Wait. Interceptor moved (7,7)→(6,4), Guard/Wait,
  and Space resolved the next enemy phase.
- In round three, Gunner's Overcharge Shot reduced Artillery (10→1 HP).
  Keyboard Guard/Finish handed off through Vanguard to Interceptor.
  Interceptor moved (6,4)→(6,2) and defeated Artillery with Pulse Carbine.
- Victory appeared in round three. The Result overlay followed playback,
  displaying MISSION COMPLETE, PRIMARY CLEAR and BONUS ACHIEVED. All pilots
  survived (Vanguard20, Gunner5, Interceptor15 HP). Clicking Continue opened
  Aftermath with Base300 + Bonus100 = Total400 and Credits400. The isolated
  save advanced to Mission Two with zero upgrades.

This completes the previously blocked native Mission 1-through-Victory
play-through on `7cd91e6`, including the Result action. Full visual parity
and the remaining campaign matrix are not yet accepted. Native findings
being resolved before capture automation: the header kept showing four
enemies after knockouts; no visible Resolve action appeared when all pilots
finished (Space worked); the LOG panel showed status/keyboard help rather
than recent battle events. A cyan-highlighted but unreachable Move cell
also needs comparison with the intended highlight contract.

Native continuation after that Victory also verified Aftermath dialogue →
Hangar → purchase Gunner HP level1 (400→200 credits) → Next Drop → Mission2
briefing → Deploy → a usable re-entered battle, where Gunner showed 18/18HP.
The isolated save now retains Mission2, 200 credits, and Gunner HP level1.
The re-entered HUD left “Loading battle UI assets...” visible after activation
and command controls became usable; this is included in the Task4 fix list.
Mission2 briefing also displayed the header “BONUS · TURNABOUT” above the
correct Hold Fast copy. That campaign-label observation is outside this
Task4 follow-up and is retained for final review.

## Native HUD fixes after the play-through — 2026-09-09

Commit `ef524e2` corrects the living enemy count, exposes the fixed-sidebar
Resolve action, removes misleading cyan on unreachable hovered cells, and
clears stale asset-loading text on battle re-entry. All 233 focused tests,
formatting, diff checks, and Clippy passed. Scoped review confirmed the state
fixes and found one Resolve-color overwrite; `5c38953` fixes it and its
scoped re-review is clean.

The log discrepancy was reproduced in the actual native renderer with a
temporary fresh Mission1 diagnostic. It performed one legal Vanguard move
to (3,6), queued its returned events, and observed production playback.
At `5c38953`, both RecentBattleLog and the visible PlaybackText contained
“Vanguard MOVING”, but the node measured 426×0 physical pixels. This isolated
the issue to layout rather than event production.

Commit `1e41ea3` removes the log text child's redundant zero-minimum-height
and clipping constraints; the existing log panel retains clipping. The focused
test, all 12 snapshot tests, formatting, Clippy, and diff checks pass. The
rebuilt native diagnostic reached the same state and reported:

```text
entries=["Vanguard MOVING"]
text="Vanguard MOVING"
computed_size=Vec2(426.0, 23.0)
content_size=Vec2(168.0, 23.0)
inherited_visible=true
```

Only the coordinator's direct diagnostic processes were stopped with Ctrl-C.
These scripted moves verify native rendering; they are not substitutes for
the separately recorded pointer/keyboard Victory play-through at `7cd91e6`.
Final source comparison and the whole-campaign acceptance matrix remain open.

The scoped log-layout review found no new issue. Native proof covers one
populated log entry; the multi-entry overflow/capture matrix remains part
of the upcoming capture and integrated acceptance work.

## Task 5 typed native capture — 2026-09-10

Task 5 adds an opt-in `ui-capture` example and a Rust const fixture table for
all 44 IDs in `docs/references/hpa-480/reference-manifest.json`. The table
uses the authored `MissionId`, unit and weapon IDs, campaign actions, battle
commands, grid positions, and typed expected facts for screen, phase, round,
menu, targeting mode, inspection, HP, objective progress, boss intent,
dialogue, credits, playback, and asset status. Each run uses a fresh isolated
temporary save path; the platform save is never opened or changed.

The capture example assigns the existing production campaign or battle camera
to a Bevy `RenderTarget::Image`, applies `UiTargetCamera` to the existing
`ViewportRoot`, and requests `Screenshot::image` after the layout and asset
readiness checks. This is native Bevy renderer output from the production UI
tree. The requested window scale override is 1.0, so the primary captures are
physical 1920×1080 pixels; `letterbox-1600x1000.png` is exactly 1600×1000.
Warmup frames are independent from presentation time. `--time-ms` advances
the pinned `Time<Virtual>` value once after fixture actions, while
`ManualDuration(Duration::ZERO)` keeps later frames from advancing it.

The final matrix command used the rebuilt example binary and wrote
`target/ui-capture/matrix-final5/`. The 43 primary scenarios were run with
`--size 1920x1080 --seed 7 --time-ms 0`; the final
`letterbox-1600x1000` scenario used `--size 1600x1000`. It produced 44 PNGs
and 44 `capture screenshot ready` records with no capture failure or panic.
Representative inspected outputs include:

- `battle-active-vanguard.png`: complete 9×9 board, blockers, tokens,
  selected Vanguard sidebar, move cells, populated threat cards, and HUD;
- `m5-overlapping-threats.png`: four real Mission 5 opening entries
  (`Controller LOCKED INTENT`, `Flanker MOVING`, `Controller MOVING`, and
  `Bulwark MOVING`) with the ordinary keyboard-ready status;
- `battle-playback.png`: four real Mission 1 opening entries and one active
  legal Vanguard movement event, while `result-victory-bonus.png` shows the
  settled result overlay;
- `asset-loading.png` and `asset-error.png`: visible loading and failed-asset
  status text after the override is reapplied after the production monitor;
- `m6-boss-low.png` and `m7-boss-low.png`: authored Dreadnought 16/40
  Overload Salvo and Regent 20/52 Rupture Beam threshold states; and
- `aftermath-m7-line3.png`: authored aftermath cursor 2, Vanguard, and
  `Copy. Mission complete.`.

The populated logs are derived from real `begin_round` events passed through
the existing `presentation::ui::format_event` formatter. `battle-playback`
keeps a real legal Vanguard `move_unit` event active; ordinary fixtures,
including Mission 5, clear playback state after applying setup while keeping
the recorded log entries. Repeating the same playback command at time zero
produced identical PNG bytes (SHA-256
`560f2c81d98715eec109ee7a9143e9628673cc1ad94ec72a0b46d73687f0ceb4` for both
captures). The same fixture at `--time-ms 250` produced a different hash
(`f444ec89d2b6810a71bea6690889b5caf25154528f9af5a4bd305d69066a9590`) and
22,376 differing pixels, proving that the requested presentation time
advances playback independently from render warmup.

`BattleState::new` is public so the separate example crate can reconstruct
typed authored board, units, weapons, and rules for renderer-only terminal and
HP fixtures. The constructor and validation behavior are unchanged; authored
definitions and public domain actions remain the source of fixture state. The
Turnabout bonus uses aligned public pushes until real collision damage marks
the authored optional objective. Boss threshold fixtures use authored Mission
6/7 rules and bounded legal campaign actions to reach the requested future
intent; they do not relabel or replace mission rules. The startup canvas fix
also makes `setup_canvas` reuse a canvas created by the initial StateTransition,
with a focused regression covering the Bevy StateTransition-before-Startup
ordering.

`tools/compare_ui.py` was checked with equal 4×3 images, reporting
`differing_pixels: 1/12` and `max_channel_delta: 5`, and with a 4×3 versus
5×3 pair, which failed explicitly with
`image sizes must match exactly: reference=4x3, actual=5x3`. The required
updated-source aligned reference PNG is not present at
`docs/references/hpa-480/aligned/battle-active-vanguard.png`, so the exact
source comparison remains an external gate. The source browser security gate
also remains unchanged; no old flat reference or synthetic baseline was used.

Native runs still print Bevy's `Skipped event Destroyed for unknown winit
Window Id` warning after a successful screenshot. The image target removes the
primary-window occlusion dependency for renderer evidence; pointer gameplay
acceptance and source comparison remain separate gates.

## Task 6 integrated acceptance — 2026-09-10

The implementation checkpoint is `cae2bb88ecbba92b893d3c7c1cec2e3e53970710`
(`fix: render targeting cancel border`). The Task 6 code commits are
`35b4d39` (typed Battle sidebar components), `4c77e78` (type-aware capture
cleanup), `273083c` (Inspector sizing and command typography), `96c6026`
(Inspector source palette and live colors), and `cae2bb8` (targeting Cancel
border). The implementation tree was clean at the checkpoint.

Task 6 restores the authored campaign bindings and styles for the briefing,
Hangar, Result, Title, Story, Aftermath, and Ending screens, including the
Mission 4 `Chain Reaction` bonus title, source gradients/fades, letter spacing,
cropped source-colored icons, numeric credits, MAX/WAIT upgrade states, and
source progress pip colors. Battle now projects the existing typed snapshots
through the source-shaped sidebar: Inspector allegiance palettes and outline,
numeric HP/EN/stat fields, icon-only accessible weapon rows, typed Preview and
Threat cards, source Cancel styling, an initially empty Log, and the guarded
Result actions. The existing command bindings, authored objective/outcome text,
committed-intent behavior, and atomic save/restart guards remain in place.

The native icon atlas at `assets/ui/icons.png` is 512×576 with SHA-256
`6d583b0a85e6a7a12fcd238307a239a7ede27cbd8905ebcc82fa1cb9dbe43c2f`.
Task 6 preserves every pre-existing slot 0–62 from BASE byte-for-byte,
including source-original slots 0–50 and the Task 5 masks 51–62. It appends
the cropped source masks in slots 63–67 for HP plus, Hangar Mobility, Ending
Mobility, Weapon lightning, and purchase currency. The same dimensions, hash,
slot map, and source paths are recorded in
`docs/references/hpa-480/reference-manifest.json`.

### Native evidence

The rebuilt capture binary was `target/debug/examples/ui_capture` with SHA-256
`a653a36f548047952cefcf0cd15edd6d8b2d35a65fe6612ea6c603d83892da4c`. The
complete final matrix is
`target/ui-capture/matrix-final6-final/`: all 44 fixture IDs from
`examples/ui_capture/fixtures.rs`, rendered with `--seed 7 --time-ms 0`,
43 at 1920×1080 and `letterbox-1600x1000` at 1600×1000. The earlier seed-0
attempt is excluded; every file in this acceptance directory belongs to the
seed-7 run. Each fixture exited successfully and printed `capture screenshot
ready`; the directory contains exactly 44 PNGs with the expected dimensions.

Representative corrected campaign outputs are retained in
`target/ui-capture/task6-fixed/` (`briefing-m7.png`, `hangar-affordable.png`,
`hangar-maxed.png`, `ending-complete.png`, and Result fixtures). The later
Battle/Result checkpoint is in `target/ui-capture/task6-proof-final/`
(`battle-active-vanguard.png`, `battle-attack-targeting-occupied.png`, and
the Result fixtures). These directories are mixed Task 6 checkpoints; the
complete final committed-SHA set is the 44-file
`target/ui-capture/matrix-final6-final/` matrix. The occupied-target proof
contains the source brown Cancel fill and its 2px red inset outline; the save
error is readable in the foreground Result card.

Playback timing was recaptured at the same implementation head in
`target/ui-capture/playback-task6-final/` using `battle-playback`, 1920×1080,
and seed 7. The two time-zero captures both hash to
`36421bbebb9cdb7df4fecb8a33603413e6a56d1b8172635cbf6a39c9d80885f1`;
the `--time-ms 250` capture hashes to
`352961c223d671f58430f3b47bc7251629ae6225dbc74f3c4a47103f6fcbe180`.
The time-zero pair proves deterministic bytes and the distinct time-250 hash
proves the playback time input is observable. The exact commands were:

```text
rtk cargo build --features ui-capture --example ui_capture
rtk target/debug/examples/ui_capture --scenario <fixture> --size 1920x1080 --seed 7 --time-ms 0 --output <path>.png
rtk target/debug/examples/ui_capture --scenario letterbox-1600x1000 --size 1600x1000 --seed 7 --time-ms 0 --output <path>.png
rtk target/debug/examples/ui_capture --scenario battle-playback --size 1920x1080 --seed 7 --time-ms 250 --output <path>.png
```

The host was macOS 26.6.2 build 25G83 on MacBookPro18,3 with Apple M1 Pro,
10 CPU cores, 14 GPU cores, 32 GB RAM, and Metal supported. Capture scale was
1.0 and the requested PNG sizes above are physical output sizes. The locked
host reported no attached display resolution or DPI through
`system_profiler SPDisplaysDataType`; physical DPI is therefore unavailable in
this record. Native output is renderer evidence from the production UI tree,
not a pointer/keyboard playthrough claim.

### Verification and limits

Focused verification at the implementation head passed:

```text
rtk cargo test --test ui_snapshots                         PASS — 14 passed
rtk cargo test --test campaign_flow                        PASS — 28 passed
rtk cargo test --test presentation_app                     PASS — 27 passed
rtk proxy cargo test --features bevy/debug --test ui_snapshots battle_sidebar_binds_typed_preview_threat_and_icon_only_weapon_rows -- --nocapture
  PASS — 1 passed; dependency debug output was used once for the Bevy B0001 diagnosis
rtk python3 tools/test_compare_ui.py                       PASS — comparator self-check
rtk cargo fmt --check                                      PASS
rtk cargo clippy --all-targets --all-features -- -D warnings PASS
rtk cargo test --all-targets                               PASS — 291 passed
rtk cargo test --all-targets --all-features                 PASS — 291 passed
rtk cargo build --release                                  PASS
```

The capture cleanup fix was verified separately. `SavePreset::Error` creates an
isolated directory at the owned save path; the prior unconditional
`remove_file` emitted an `Operation not permitted` warning on macOS. Cleanup
now checks `is_dir()` and calls `remove_dir`, otherwise `remove_file`, with the
existing NotFound handling and real-error warning retained. A fresh
`result-save-error` capture completed without the cleanup warning and left no
new save directory. The known Bevy unknown-window `Destroyed` teardown warning
still follows successful captures and is disclosed rather than suppressed.

This is `DONE_WITH_CONCERNS` for local integrated acceptance. The updated-source
and aligned PNGs are still unavailable, so no source-image comparison or
synthesized baseline is claimed; browser local-file navigation was rejected by
the browser security policy and no alternate browser/local server/indirect
workaround was used. The Mac is currently locked, so the requested native
pointer/keyboard seven-mission run, final save/reload/restart playthrough, and
platform acceptance remain open. The plan's external PR/HPA completion status
is intentionally unchanged.

## Task 6 fix round 1 — 2026-09-10

Aggregate review identified two source-parity gaps at the prior local
checkpoint. Implementation commit `ba813dd8ce4aa587d7953b119680248eac25a9d3`
corrects both in one batch:

- The `PreMissionStory` overlay now uses source `#05080f` for its 0% and 45%
  stops, retaining `#03060c` only at 100%.
- Terminal Result **CONTINUE** and **RETRY** actions now use source
  `#0e2c3a` and a 1px accent inset border. The existing live updater applies
  this only to the Result actions, leaving the Header Restart and other
  command controls unchanged.

The existing `tests/ui_snapshots.rs` terminal tests were extended to assert the
live post-update CTA background, accent `BorderColor`, and 1px `Node.border`
for both the victory Continue and defeat Retry variants. Existing terminal
visibility, guarded action, save-error, and inactive-action assertions remain
in the same suite.

Fix-round checks:

```text
rtk cargo test --test ui_snapshots battle_result_snapshot -- --nocapture
  PASS — 3 passed, 11 filtered out
rtk cargo test --test ui_snapshots
  PASS — 14 passed
rtk cargo fmt --check
  PASS
rtk cargo clippy --all-targets --all-features -- -D warnings
  PASS — no issues found
rtk cargo build --features ui-capture --example ui_capture
  PASS
```

Affected native proof was captured at `ba813dd` in
`target/ui-capture/task6-fix1/` using seed 7, 1920×1080, and time 0:

```text
story-m1-line1.png       03ed7173f92b4523b8f1667d81784d550b6ae41a715ca9354b5558537f6ae7ba
result-victory-bonus.png c0bb786fa2c0b036e9e07b050080035405986ff2b7feb86ffbd9b419fa20a7a5
result-defeat.png        9ada59df699f63e011f84c5dbd7b8144c454fafcf9390643513b5ab4af5dd686
result-save-error.png    63278b5dc0b35ad8d9f26ff83a22a74a16e1861deb641dd5b02d74eca4e0284f
```

All four runs exited successfully and printed `capture screenshot ready`; the
save-error run emitted no cleanup warning. The known Bevy unknown-window
`Destroyed` teardown warning still follows successful captures. The earlier
`target/ui-capture/matrix-final6-final/` 44-image seed-7 matrix and
`target/ui-capture/playback-task6-final/` timing proof remain valid for their
unchanged scenes because this fix changes only the Story overlay stops and
terminal Result CTA chrome. No entire matrix rerun or new baseline was used.
