# HPA-632 validation

## Bevy viability checkpoint — 2026-08-23

Environment:

- Rust: `rustc 1.96.0 (ac68faa20 2026-05-25)`
- Bevy: `0.19.1` from `Cargo.lock`
- Host: macOS native window, Apple Silicon

### Required evidence

- [x] A normal Bevy application starts from `cargo run` with `DefaultPlugins` and a 1280×720 native window.
- [x] An orthographic `Camera3d` renders a real 3D battlefield with lit, shadow-casting meshes.
- [x] The checked-in `assets/models/mission_one.gltf` loads through `AssetServer`; scene 0 renders the original low-poly Vanguard model.
- [x] Native Bevy UI renders the checkpoint title, instructions, and selected logical cell.
- [x] Bevy 0.19 opt-in mesh picking maps a real pointer click on the center mesh to logical cell `(1, 1)`.
- [x] A second pointer click on adjacent cell `(1, 0)` mutates canonical `BattleState`, and `UnitVisual(UnitId(1))` follows the canonical position in 3D.
- [x] Domain movement and the canonical-state-to-transform handoff run in automated tests without Winit, a window, or a renderer.

### Native observation

A bounded native launch exercised the same debug binary built by Cargo. Before interaction, the UI reported `Selected cell: none` and the Vanguard stood on the center cell. The first mesh click changed the UI to `Selected cell: (1, 1)`. The adjacent mesh click changed it to `Selected cell: (1, 0)` and visibly moved the Vanguard to that cell. The driver terminated the app after the capture sequence.

Local capture artifacts from this run:

- `/private/tmp/scorpius-before-click.png`
- `/private/tmp/scorpius-after-center-click.png`
- `/private/tmp/scorpius-after-adjacent-click.png`
- `/private/tmp/scorpius-checkpoint.log`

### Commands

```text
python3 -m json.tool assets/models/mission_one.gltf
cargo check --all-targets
cargo run
cargo test --test presentation_app canonical_move_drives_visual_transform_without_renderer
cargo test --test presentation_app adjacent_cell_click_moves_canonical_unit
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-targets
cargo build
cargo build --release
swift /private/tmp/run_scorpius_checkpoint.swift \
  target/debug/scorpius /private/tmp "$PWD"
```

Final gate results:

- `cargo fmt --check`: passed
- strict Clippy: passed with no findings
- `cargo test --all-targets`: 5 passed across 3 suites
- `cargo build --release`: passed; 347 crates compiled in 5m 58s

The glTF JSON validation, compilation, focused renderer-free tests, complete automated suite, debug and release builds, native launch, mesh clicks, UI updates, and visual synchronization all passed.
