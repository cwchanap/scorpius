# HPA-637 validation

Validation baseline: live Linear issue `HPA-637`, refreshed 2026-08-28. Product commit under test: `327841fcf6e3073b7f268f77f0659535988bd36a`.

## Environment

- Host: 2021 MacBook Pro, Apple M1 Pro, 32 GB RAM, arm64
- OS: macOS 26.5.2 (25F84)
- Rust: `rustc 1.96.0 (ac68faa20 2026-05-25)`
- Bevy: `0.19.1` from the committed `Cargo.lock`

## Gate results on the product commit

- `cargo fmt --check`: passed
- `cargo clippy --all-targets --all-features -- -D warnings`: passed, no findings
- `cargo test --all-targets`: **165 passed, 0 failed** across five suites — 107 lib unit tests, 19 `campaign_flow`, 1 `campaign_model`, 21 `campaign_persistence`, 17 `presentation_app`
- `cargo build --release`: passed

## Lifecycle and push evidence (automated, domain + campaign tests)

**Mission 2 — Hold Relay Nine (Protect Gunner):**

- Immediate-clear win before the deadline with attackers remaining: `clearing_every_attacker_wins_immediately_before_round_three`; shared rule coverage in `protect_rules_win_immediately_when_all_attackers_die`.
- Round-3 win with an attacker still alive: `surviving_to_round_three_wins_with_an_attacker_still_alive`; boundary-only-then coverage in `protect_rules_win_at_round_three_and_only_then`.
- Gunner KO fails the mission: `gunner_ko_fails_the_mission`, plus `protect_rules_fail_when_the_target_dies`.
- Hold Fast bonus at the 50%-of-15-HP boundary: `hold_fast_bonus_tracks_the_fifteen_hp_half_boundary`.

**Mission 3 — Intercept Courier (race to the exit):**

- Player-4 timing: the Courier's first three moves leave the player phase of Round 4 alive and un-extracted — `the_couriers_first_three_moves_leave_player_phase_four_without_extraction` (intermediate distances are not pinned, per spec).
- Live move-4 extraction defeat: `the_couriers_fourth_move_reaches_extraction_and_fails_the_mission`, with rule-level coverage in `intercept_rules_fail_when_the_courier_reaches_extraction`.
- Blocked Round-5 fallback: with the exit occupied, the Courier stalls and only the Round-5 deadline ends it — `an_occupied_exit_stalls_to_the_round_five_deadline`.
- Push-to-exit loss: `pushing_the_courier_onto_extraction_fails_immediately` — resolving a push onto the extraction cell is an immediate defeat.
- Courier-only win and escort-clear non-win: `courier_ko_wins_even_with_escorts_standing`, `killing_both_escorts_alone_does_not_end_the_battle`, `intercept_rules_win_when_the_courier_dies`.
- Swift Intercept bonus boundary: `swift_intercept_bonus_tracks_the_round_two_boundary`.

**Flanker planner and initiative:**

- Protect pressure hugs the protected unit's weapon band and falls back when the Gunner is out of any legal footprint: `flanker_protect_pressure_hugs_the_protected_units_weapon_band`, `flanker_protect_intent_falls_back_when_gunner_is_out_of_footprint`, `flanker_fallback_intent_keeps_shared_priority_targeting`, `flanker_protect_intent_locks_the_protected_gunner_over_shared_priority`.
- Courier pressure races Manhattan distance to the escape point: `flanker_courier_pressure_races_manhattan_to_the_escape_point`.
- Tie-break band → Manhattan → row → column, no x-position hack: `flanker_protect_tie_breaks_band_then_manhattan_then_row_then_column`.
- Fallback for a Flanker with no pressure rule reuses the attack band: `flanker_without_a_pressure_rule_reuses_the_attack_band_fallback`.
- Initiative is fixed Striker 30 / Flanker 25 / Rifleman 20 / Artillery 10 with no positional field: `flanker_initiative_slots_between_striker_and_rifleman`, `initiative_is_fixed_per_archetype_without_position`.

**Turnabout gate (M1 bonus kept special, other optionals ignore the trigger):**

- `enemy_or_environment_damage_to_enemy_completes_turnabout`, `player_weapon_damage_alone_does_not_complete_turnabout`, `non_turnabout_optionals_ignore_the_enemy_damage_trigger`.

**Campaign One→Four, 1200/800 credits, save/load:**

- Base rewards alone reach Mission 4 (300 + 400 + 500 = 1200): `campaign_progresses_through_four_on_base_rewards_alone`.
- A base-only run to Mission 4 with two 200-credit purchases round-trips exactly 800 credits: `base_only_run_to_four_with_two_purchases_round_trips_800_credits`.
- Save/Continue routing by saved `next_mission`, including Three→upgrade and Four→handoff: `title_continue_routes_by_the_saved_next_mission`, `continue_and_proceed_route_three_to_upgrade_and_four_to_the_handoff`.
- Upgrades persist: `save_at_four_round_trips_purchased_upgrades`, `purchase_upgrade_action_persists_and_reports_failures`.
- Mission 2/3 battle entry and restart through the shared definition path with upgrades: `mission_two_entry_and_restart_run_the_shared_definition_path_with_upgrades`, `mission_three_entry_builds_through_the_shared_definition_path_with_upgrades`, `battle_entry_builds_the_active_mission_with_campaign_upgrades`.

**Authoring legality and HUD:**

- Spec boards/rosters and legal opening destinations: `mission_two_authors_the_spec_board_and_roster`, `mission_three_authors_the_spec_board_and_roster`, `mission_two_opening_rows_reference_legal_units_and_destinations`, `mission_three_opening_rows_reference_legal_units_and_destinations`, `mission_three_extraction_is_a_legal_open_cell_and_the_race_is_manhattan_fourteen`.
- Boundary helper semantics: `completed_enemy_round_requires_enemy_planning_and_the_reached_round` (EnemyPlanning phase and reached round both required).
- M2 HUD Round n/3 + Gunner HP and M3 HUD Round n/5 + exit distance: `hud_tracks_protect_mission_round_cap_and_gunner_hp`, `hud_tracks_intercept_mission_round_cap_and_courier_distance_to_exit`; extraction ring reuses the existing white ring material: `intercept_mission_spawns_one_white_extraction_ring_at_the_escape_cell`; non-intercept missions spawn none: `missions_without_an_intercept_primary_spawn_no_extraction_marker`.

## glTF evidence

`flanker_scene_is_authored_with_own_mesh_material_and_root_scale` (inline test in `src/presentation/assets.rs`) asserts the checked-in `assets/models/mission_one.gltf` JSON directly:

- scene index 10 named `Flanker`; total scene count 11.
- scene 10 roots at node 49; nodes total 56 with part nodes 50–55 as children of 49.
- root node 49 carries authored scale `[0.72, 0.72, 0.72]` — no presentation-side scale workaround, no `unit_scale`, no child under-ring.
- mesh 10 and material 10 both named `Flanker Magenta`; primitives reference material 10 and reuse the existing shared POSITION/NORMAL accessors.
- exactly one buffer — the Flanker adds no new bin file.

## Mission 1 regression evidence

The Mission 1 opening moved onto `EnemyOpening` data with behavior asserted unchanged:

- `authored_opening_places_four_locked_threats` — same four locked threats, footprints, and order as before the refactor.
- `mission_one_opening_rows_reference_legal_units_and_destinations` and `mission_one_has_the_locked_roster_and_nine_player_weapons` — roster, weapons, and deployment unchanged (`upgraded_construction_keeps_the_mission_one_deployment`).
- `full_enemy_phase_requires_finished_players_and_preserves_intent_order` — committed-intent ordering survives the initiative change; Rifleman positional initiative hack removed with ordering covered by `initiative_is_fixed_per_archetype_without_position`.
- `board_layout_matches_the_approved_coordinates` and `weapon_values_match_the_approved_design` — board and combat data untouched.

## Manual playtest — PENDING-HUMAN

**The items below have NOT been run.** This session could not launch the GUI; every checkbox is honest pending state, not missing evidence to backfill silently. Run each in the release binary and record the result with a capture where useful.

Mission 2 — Hold Relay Nine:

- [ ] Competing threats opening — pending human playtest. Look for: four locked telegraphs (Rifleman/Striker/Artillery/Flanker) visible at round 1 with the Gunner under overlapping threat lines.
- [ ] Clear win — pending human playtest. Look for: eliminating all attackers ends the mission immediately, before Round 3.
- [ ] Round-3 win — pending human playtest. Look for: surviving with the Gunner alive flips to victory as Round 3 completes even with an attacker standing.
- [ ] KO fail — pending human playtest. Look for: Gunner knockout produces the defeat overlay even with other units alive.
- [ ] Hold Fast bonus — pending human playtest. Look for: bonus paid at exactly half HP and denied one point below.

Mission 3 — Intercept Courier:

- [ ] Magenta Courier visible — pending human playtest. Look for: the Flanker-piloted Courier renders in its own magenta material at ~72% of the other units' size (authored root scale 0.72, by design — confirm the lighter frame reads as intended rather than broken).
- [ ] White ring at the exit — pending human playtest. Look for: one white extraction ring at (8, 0) and none on other missions.
- [ ] Player-4 pacing — pending human playtest. Look for: three unblocked Courier moves hand the player exactly Round 4 to close the gap.
- [ ] Open-route extraction — pending human playtest. Look for: the untouched Courier extracts on its fourth move with the defeat overlay.
- [ ] Blocked Round-5 fallback — pending human playtest. Look for: a unit parked on the exit stalls the Courier until the Round-5 deadline defeat.
- [ ] Courier-only win — pending human playtest. Look for: killing the Courier wins immediately while escorts still stand.
- [ ] Swift Intercept bonus — pending human playtest. Look for: bonus paid only for a Round-2 victory.

Campaign shell:

- [ ] Save / Continue — pending human playtest. Look for: victory persists `next_mission`/credits/upgrades; CONTINUE resumes at the right screen per mission and never replays a reward.
- [ ] Upgrades — pending human playtest. Look for: purchases deduct credits, persist across restart, and apply to the next mission build.
- [ ] Mission-4 handoff — pending human playtest. Look for: Mission 3 victory leads through aftermath/upgrades to the final MISSION 4 UNLOCKED handoff screen and stops there.

## Acceptance matrix

- [x] **M2 immediate-clear and Round-3 paths are both correct, with KO fail and bonus boundary.** Evidence: the four Mission 2 tests named above.
- [x] **M3 Player-4 timing, live move-4 extraction, Round-5 blocked fallback, and push-to-exit loss are proved without pinning intermediate distances.** Evidence: the Mission 3 tests named above.
- [x] **Flanker planner fallback, 30/25/20/10 initiative, and tie-breaks work without an x-position hack.** Evidence: the Flanker planner/initiative tests named above.
- [x] **Authoring legality for both new boards and openings is tested; no runtime validation framework was added.** Evidence: the authoring-legality tests named above.
- [x] **The Flanker glTF scene is authored in place: scene 10 / count 11 / nodes 49–55 / mesh+material 10 "Flanker Magenta" / root 0.72 / single buffer.** Evidence: `flanker_scene_is_authored_with_own_mesh_material_and_root_scale`.
- [x] **M2 HUD n/3 + Gunner HP and M3 HUD n/5 + exit distance use generic copy.** Evidence: the HUD tests named above.
- [x] **One→Two→Three→Four campaign flow with 1200 base credits and intact save/upgrades.** Evidence: the campaign tests named above.
- [x] **Mission 1 openings/order unchanged after the `EnemyOpening` refactor; one boundary helper (`completed_enemy_round`).** Evidence: the Mission 1 regression tests named above.
- [x] **`cargo fmt --check`, strict Clippy, `cargo test --all-targets`, and `cargo build --release` pass.** Evidence: gate results above (165 tests). CI run still pending a configured remote, same as prior tickets.
- [ ] **Manual playtest confirms readability, pacing, and the campaign handoff in the live GUI.** Evidence: PENDING-HUMAN — the checklist above has not been executed; this is the only open acceptance item.

## Verification commands

```text
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-targets
cargo build --release
```

All four pass on the product commit; counts are recorded under Gate results. The remaining work is the pending-human manual playtest checklist above.
