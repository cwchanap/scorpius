# HPA-386 validation

Validation baseline: live Linear issue `HPA-386`, recorded 2026-09-03. Implementation head under test: the five HPA-386 implementation commits, through `d859c73` (`feat: polish board-first combat feedback`) — `6c7233a` (`feat: add Regent boss behavior`), `a4ce960` (`feat: author Mission 7 and campaign ending`), `a780ab0` (`fix: announce campaign completion on the ending screen`), `bcfa7c0` (`feat: add Regent battlefield visual`), `d859c73`.

**Method disclosure.** This session is a headless coding agent: no GUI, no live renderer, no wall-clock playthrough. Following the HPA-523/HPA-637 precedent, every fact below is (a) automated evidence from the merged test suite and gates — all of it re-run and re-verified on this tree — or (b) an explicit `PENDING-HUMAN` placeholder for the manual playthrough. No timing, round count, restart count, bonus result, or purchase was invented, and **no tuning was performed** (Step 7's precondition — recorded playtest evidence — cannot exist yet).

## Environment

- Host: 2021 MacBook Pro (MacBookPro18,3), Apple M1 Pro, 32 GB RAM, arm64
- OS: macOS 26.6.2 (25G83)
- Rust: `rustc 1.96.0 (ac68faa20 2026-05-25)`
- Bevy: `0.19.1` from the committed `Cargo.lock`

## Gate results on the implementation head (`d859c73`)

- `cargo fmt --check`: passed
- `cargo clippy --all-targets --all-features -- -D warnings`: passed, no findings
- `cargo test --all-targets`: **246 passed, 0 failed** across five suites — 173 lib unit tests, 25 `campaign_flow`, 1 `campaign_model`, 24 `campaign_persistence`, 23 `presentation_app`
- Focused re-runs recorded below under each section. Full gates get re-run in the HPA-386 closeout task; the numbers above are this session's honest local snapshot, not a CI record.

## Campaign playthrough

| Mission | Minutes | Rounds | Restarts | Bonus? | Intent manipulation materially rewarded? | Notes / tuning |
| --- | ---: | ---: | ---: | --- | --- | --- |
| 1 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 2 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 3 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 4 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 5 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 6 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |
| 7 | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | PENDING-HUMAN | |

## Progression ledger

Credit *rewards* below are the authored typed constants, verified in source and pinned by tests (`new_game_and_mission_one_definition_are_locked` in `tests/campaign_model.rs` pins every base/optional reward pair and the 3300 cumulative base; `completing_three_four_and_five_advances_to_six_then_six_advances_to_seven_at_3300` in `tests/campaign_flow.rs` walks the real progression). **Credits-after and upgrade purchases are player choices — PENDING-HUMAN.** Optional reward is paid only on the bonus condition; the base-only column is what a no-bonus run earns.

| Transition | Credits before | Reward (base + optional) | Upgrade purchases | Credits after |
| --- | ---: | --- | --- | ---: |
| New Game -> M1 | 0 (pinned) | — | PENDING-HUMAN | 0 |
| M1 -> M2 | 0 | 300 + 100 | PENDING-HUMAN | PENDING-HUMAN |
| M2 -> M3 | PENDING-HUMAN | 400 + 100 | PENDING-HUMAN | PENDING-HUMAN |
| M3 -> M4 | PENDING-HUMAN | 500 + 150 | PENDING-HUMAN | PENDING-HUMAN |
| M4 -> M5 | PENDING-HUMAN | 600 + 150 | PENDING-HUMAN | PENDING-HUMAN |
| M5 -> M6 | PENDING-HUMAN | 700 + 200 | PENDING-HUMAN | PENDING-HUMAN |
| M6 -> M7 | PENDING-HUMAN | 800 + 250 | PENDING-HUMAN | PENDING-HUMAN |
| M7 -> Ending | PENDING-HUMAN (3300 on a base-only, no-purchase run — pinned) | 1000 + 300 | — | PENDING-HUMAN |

Authored constants: base 300/400/500/600/700/800/1000 for Missions One–Seven (`base_reward` in `src/mission/mission_one.rs` through `mission_seven.rs`); optional 100/100/150/150/200/250/300. Base-only run through Six totals exactly 3300; the maximum at Seven entry (every bonus through Six claimed, no purchases) is 3300 + 950 = 4250 — both pinned by `new_game_and_mission_one_definition_are_locked`. Upgrade costs 200/400/600: `upgrade_costs_are_200_400_600` (`tests/campaign_persistence.rs`).

## Total time

- PENDING-HUMAN — requires the live playthrough.

## Boss-threshold timing (Regent)

Automated evidence (re-run green this session):

- Weapon switches exactly at half HP: `both_bosses_switch_at_their_exact_half_hp_boundary` (`src/domain/enemy.rs`) — Command Barrage at 27 HP, Rupture Beam at 26 (half of 52); Dreadnought re-pinned alongside.
- Committed intent is immutable across the crossing: `regent_threshold_crossing_changes_only_future_intents` — the committed Command Barrage intent stays identical after damage drops the Regent to 26; only the next `build_intent` picks Rupture Beam.
- Initiative: Regent 45 > Dreadnought 40 > Controller 35 — `initiative_is_fixed_per_archetype_without_position`.
- Authored profiles: Command Barrage range 3–6 Cross1, Rupture Beam range 2–4 Single — `mission_seven_pins_the_enemy_roster_and_regent_weapon_profiles` (`src/mission/mission_seven.rs`).

When the threshold is actually crossed during a live fight (round number, whether the player engineered it, readability of the swap moment): PENDING-HUMAN.

## Telegraph readability

- PENDING-HUMAN — requires the live playthrough. Automated support: the seed-2 regression's committed footprints (see Mission 7 geometry below) and the M6-era in-domain readability pin pattern (`redirected_opening_ready_to_resolve` asserts the committed Cross1 footprint cells before resolution).

## Presentation observations

- PENDING-HUMAN — screen readability, pacing feel, overlay behavior in the live GUI.

Automated support for the feedback layer is in the EventPlayback section below.

## Mission 7 geometry (automated)

All confirmed by the seed-2 public-path regression in `src/mission/mission_seven.rs`, re-run this session (`cargo test --lib mission::mission_seven`: **6 passed, 0 failed**):

- **Regent footprint contains explosive (3,7) and empty push cell (5,7).** `mission_seven_authors_the_spec_board_boss_and_rules` pins the explosive at `(3,7)` (HP 4) and no explosive at `(5,7)`; the shared fixture `redirected_opening_ready_to_resolve` asserts the committed Command Barrage footprint contains both `(3,7)` and `(5,7)` after a real `begin_round()`.
- **Vector Pulse moves Controller to (5,7).** The fixture fires the real `BattleState::attack` Vector Pulse (spending the two seeded rolls: hit roll 11, crit roll 27, 9 → 6 HP) and asserts the `UnitPushed` event lands the Controller on `(5,7)`; `redirected_command_barrage_detonates_the_explosive_and_cancels_the_pushed_controller` resolves from that state.
- **Explosive remains at (3,7).** The fixture asserts the `(3,7)` explosive is unexploded pre-resolution; the explosion events name `(3,7)`, not `(5,7)` (below).
- **Regent barrage damages Controller.** The regression pins the `AttackRolled` event: attacker REGENT, weapon COMMAND_BARRAGE, target CONTROLLER, roll 52, `hit: true`.
- **ExplosiveDamaged and ExplosionTriggered both name (3,7).** Both event assertions in `redirected_command_barrage_detonates_the_explosive_and_cancels_the_pushed_controller` match `position == GridPos::new(3, 7)`.
- **Controller intent cancels after knockout.** `resolve_intent` (`src/domain/enemy.rs`) emits `IntentCanceled` only for a knocked-out attacker; the regression asserts `IntentCanceled { attacker: CONTROLLER }`, proving the Vector Pulse + barrage combo KO'd the Controller before its committed push resolved.

Supporting authored-data pins: `mission_seven_opening_rows_match_the_spec`, `mission_seven_opening_rows_are_legal`, `mission_seven_definition_is_the_authored_terminal_mission`.

## Campaign terminal guard (automated)

Re-run green this session (`cargo test --test campaign_flow --test campaign_persistence --test campaign_model`: **25 + 24 + 1 passed, 0 failed**):

- **Truth table** — `campaign_destination_pins_the_terminal_routing_truth_table` (`src/presentation/campaign_ui.rs`, inline test): completed Continue/AdvanceAftermath/Proceed → `Ending`; unfinished One Continue → `PreMissionStory`; unfinished Seven Continue/AdvanceAftermath → `Upgrade`; unfinished Seven Proceed → `PreMissionStory`. The completed branches can never route back into Mission 7 story.
- **Integration routing through `apply_campaign_action`** — `completed_campaign_routes_continue_aftermath_and_proceed_to_ending` (`tests/campaign_flow.rs`): seeds a real Seven completion through `complete_current_mission`, then walks completed Continue → Ending, the final aftermath's three lines → Ending, and completed Proceed → Ending. `continue_and_proceed_route_unfinished_missions_to_upgrade_and_story` pins unfinished Seven Continue → Upgrade and unfinished Seven Proceed → PreMissionStory (Seven is authored, so it joins the regular authored-mission routing).
- **Persistence round-trips** — `seven_terminal_state_round_trips_with_upgrades_and_credits` (`tests/campaign_persistence.rs`): a `next_mission: Seven` save with upgrades and credits stores/loads byte-identical; `ending_copy_announces_campaign_complete` and `ending_copy_lists_credits_and_all_upgrade_levels` pin the Ending screen copy (`a780ab0`).
- Seven is authored and terminal: `mission_seven_definition_is_the_authored_terminal_mission` and `new_game_and_mission_one_definition_are_locked` (`unlocks: None`).

## EventPlayback feedback lifecycle (automated)

Re-run green this session (`cargo test --lib presentation::playback`: **4 passed, 0 failed**):

- **Damage-number spawn / animate / despawn lifecycle** — `damage_number_lifecycle_spawns_animates_and_despawns` (`src/presentation/playback.rs`): `spawn_damage_number` creates one `-7` text at the target origin; mid-animation the node rises 24 px toward the origin-relative top (240 → 228); playback cleanup despawns it — numbers do not accumulate.
- **Attacker pulse leaves no stale scale** — `attack_pulse_pins_base_scale_at_start_and_end_and_peaks_midway`: `attack_scale` is exactly `UNIT_SCALE` at progress 0 and 1, above it only midway.
- **Boss shake returns exactly to rest** — `boss_camera_shake_peaks_midway_and_returns_to_rest`: rest transform at progress 0 and an end transform within 1e-6 of rest; peaks only midway. "Modest" is a visual judgment — manual checklist.
- **Existing 3D impact mesh still appears** — `spawn_event_effect` keeps the "Combat impact" `WorldAssetRoot` (scene 9) spawn on the effect path asserted by the lifecycle suite; the four glTF scene tests in `src/presentation/assets.rs` re-verified (`cargo test --lib presentation::assets`: 4 passed) — Regent scene 14, `Regent Violet` mesh/material, single buffer.
- **No battle Camera2d/Text2d** — structural check (no dedicated test exists): grep for `Camera2d|Text2d` across `src/presentation/{battlefield,playback,ui,interaction,sync}.rs` and `src/domain/` returns zero matches; the battle camera is the single `Camera3d` (`src/presentation/battlefield.rs`), and damage numbers are Bevy UI `Text` + `Node` (`spawn_damage_number`), not `Text2d`. `Camera2d` exists only in the non-battle campaign screens (`src/presentation/campaign_ui.rs`).

HUD target pin for Mission 7: `mission_seven_target_hud_pins_the_regent` (`tests/presentation_app.rs`).

## Manual playtest — PENDING-HUMAN

**The items below have NOT been run.** This session could not launch the GUI; every checkbox is honest pending state, not missing evidence to backfill silently. Run each in the release binary (`cargo run`) and record the result immediately after each mission. **No tuning is authorized until these are recorded** (brief Step 7's precondition).

Per-mission rows (one checkbox per mission, fill the Campaign playthrough table):

- [ ] Mission 1 — record wall-clock minutes, rounds, restarts, bonus result, purchase, and whether intent manipulation materially changed the outcome — PENDING-HUMAN. Look for: Turnabout telegraphs readable; reward receipt matches the ledger row.
- [ ] Mission 2 — same row — PENDING-HUMAN. Look for: Gunner-protection pressure legible against the round cap.
- [ ] Mission 3 — same row — PENDING-HUMAN. Look for: Courier race pacing hands the player a real decision each round.
- [ ] Mission 4 — same row — PENDING-HUMAN. Look for: Bulwark/target tracker readable; both environmental solutions usable live.
- [ ] Mission 5 — same row — PENDING-HUMAN. Look for: crossfire footprints readable; displacement play feel fair.
- [ ] Mission 6 — same row — PENDING-HUMAN. Look for: Dreadnought threshold swap visible in play; committed-intent lock respected.
- [ ] Mission 7 — same row — PENDING-HUMAN. Look for: Regent barrage footprint readable; explosive redirect line usable in real play.

Intent-manipulation assessment (needs ≥ 4 of 7 `yes` rows in the table):

- [ ] Count and record the `yes` rows — PENDING-HUMAN. Look for: a concrete move/event per row where reading or manipulating committed intent materially changed the tactical outcome.

Regent-specific:

- [ ] Readable threshold moment — PENDING-HUMAN. Look for: the Command Barrage → Rupture Beam swap is noticeable in play without being confusing (automated boundary: `both_bosses_switch_at_their_exact_half_hp_boundary`).
- [ ] Damage number placement/readability — PENDING-HUMAN. Look for: numbers appear near the struck target, remain legible during the rise, and never overlap into noise (automated lifecycle: `damage_number_lifecycle_spawns_animates_and_despawns`).
- [ ] Boss shake is modest and the camera returns exactly to rest — PENDING-HUMAN. Look for: shake reads as weight, not nausea; no residual offset after the attack (automated return-to-rest: `boss_camera_shake_peaks_midway_and_returns_to_rest`).

Campaign shell:

- [ ] Full New Game → Ending → Title run through the real UI — PENDING-HUMAN. Look for: every authored screen in order, no soft locks, no replayed rewards.
- [ ] Completed Continue reaches Ending — PENDING-HUMAN. Look for: a completed save resumed from the Title lands on the Ending screen (automated: `campaign_destination_pins_the_terminal_routing_truth_table`, `completed_campaign_routes_continue_aftermath_and_proceed_to_ending`).
- [ ] Completed Aftermath advance reaches Ending — PENDING-HUMAN. Look for: the final aftermath walks its lines once, then Ending.
- [ ] Completed Proceed reaches Ending, never Mission 7 story — PENDING-HUMAN. Look for: no route back into the Seven pre-mission story after completion.
- [ ] Unfinished Seven Continue → Upgrade — PENDING-HUMAN. Look for: the Seven save continues into the upgrade pass (automated: `continue_and_proceed_route_unfinished_missions_to_upgrade_and_story`).
- [ ] Unfinished Seven Proceed → PreMissionStory — PENDING-HUMAN. Look for: the handoff proceeds into Seven's story.
- [ ] Save/continue behavior across the run — PENDING-HUMAN. Look for: credits and upgrades persist across restarts and Continue; receipts match the ledger.

## Acceptance matrix

- [x] **Mission 7 geometry: Regent footprint over explosive (3,7) and push cell (5,7); Vector Pulse redirect; (3,7)-named explosion events; Controller KO intent cancel.** Evidence: `redirected_command_barrage_detonates_the_explosive_and_cancels_the_pushed_controller` and the seeded fixture, re-run green.
- [x] **Campaign terminal guard: completed Continue/Aftermath/Proceed → Ending, never back into Seven; unfinished Seven → Upgrade/PreMissionStory.** Evidence: the routing truth-table and integration tests above, re-run green.
- [x] **EventPlayback feedback: damage-number lifecycle, no stale pulse scale, camera returns to rest, no battle Camera2d/Text2d.** Evidence: the playback tests and structural check above, re-run green.
- [x] **Progression rewards are the authored constants (base 300+400+500+600+700+800+1000; 3300 base before Seven).** Evidence: `new_game_and_mission_one_definition_are_locked`, `completing_three_four_and_five_advances_to_six_then_six_advances_to_seven_at_3300`.
- [x] **Regent threshold behavior (exact half-HP switch, future-intents-only, initiative 45).** Evidence: the domain tests under Boss-threshold timing.
- [x] **`cargo fmt --check`, strict Clippy, and `cargo test --all-targets` pass on `d859c73`.** Evidence: gate results above (246 tests). CI re-verification happens in the closeout task.
- [ ] **Manual playtest confirms pacing, readability, intent-manipulation reward (≥ 4/7), and campaign shell behavior in the live GUI.** Evidence: PENDING-HUMAN — the checklist above has not been executed; this is the only open acceptance item.

## Verification commands

```text
cargo test --lib mission::mission_seven
cargo test --lib presentation::playback
cargo test --lib presentation::assets
cargo test --lib -- both_bosses_switch_at_their_exact_half_hp_boundary regent_threshold_crossing_changes_only_future_intents initiative_is_fixed_per_archetype_without_position
cargo test --test campaign_flow
cargo test --test campaign_persistence
cargo test --test campaign_model
cargo test --test presentation_app
cargo test --all-targets
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
```

All pass on `d859c73`; counts under Gate results and each section. The remaining work is the pending-human manual playtest checklist above — tuning (brief Step 7) is blocked until it is recorded.

## Final automated gates

Re-run fresh on the final implementation head `b6c20f1` (`fix: restore Mission 7 authored start cells and refresh closeout gates` — restores the Bulwark/Flanker start cells to the design spec and the matching roster-pin assertions on top of `4923a06`'s legacy-save persistence fix), 2026-09-04. PR CI has now run on the pushed head; both jobs are green.

- `cargo fmt --check`: **PASS** (exit 0)
- `cargo clippy --all-targets --all-features -- -D warnings`: **PASS** (exit 0, no findings)
- `cargo test --all-targets`: **PASS — 247 passed, 0 failed** (173 lib unit tests, 25 `campaign_flow`, 1 `campaign_model`, 25 `campaign_persistence`, 23 `presentation_app`)
- `cargo llvm-cov --all-targets --lcov --output-path lcov.info`: **PASS** (exit 0; same 247 tests, 0 failed; `lcov.info` written, untracked)
- `cargo build --release`: **PASS** (exit 0)
- PR `Build + lint`: **PASS** — conclusion `success` on `b6c20f1` (workflow run 33848537244, job 100945837763)
- PR `Unit test`: **PASS** — conclusion `success` on `b6c20f1` (workflow run 33848537244, job 100945837960)
